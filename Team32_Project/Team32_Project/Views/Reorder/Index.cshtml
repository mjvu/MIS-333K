@model IEnumerable<Team32_Project.Models.Reorder>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReorderType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReorderType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReorderID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReorderID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReorderID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
